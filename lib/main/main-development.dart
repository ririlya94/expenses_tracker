import 'package:flutter/material.dart';
import '../common.dart';
import 'FlavorConfig.dart';

void main() {
  FlavorConfig(flavor: Flavor.DEVELOPMENT);
  // initializeApp();
  runApp(const MyApp());
}